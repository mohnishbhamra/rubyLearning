This repo will contains basic codes to remeber and learn ruby.
Install ruby on windows and post install run 1,2,3 installer from terminals.
Ruby comes auto installed on OSx, check the version and install updates.

check the version by "ruby -v" command

install atom IDE from atom.io



FOR VScode
1. install extention "ruby solargraph"
2. on terminal, type "gem install solargraph"
3. update doc using "solargraph download-core"
4. install code runner to run code in vscode. to run code , open .rb file and right click to run.
